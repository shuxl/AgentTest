# 前端项目

## 📋 项目简介

这是项目的前端应用，基于 React + TypeScript + Vite 构建，提供智能体对话和数据展示功能。

**特点**：
- 🎯 可同时对接 Python 后端和 Java 后端
- 🔧 通过环境变量灵活配置后端服务地址
- 📦 独立的前端项目，位于项目根目录

## 🚀 快速开始

### 环境要求

- **Node.js**: >= 18.0.0
- **包管理器**: npm / yarn / pnpm（推荐使用 pnpm）

### 安装步骤

#### 1. 安装依赖

```bash
# 使用 npm
npm install

# 或使用 yarn
yarn install

# 或使用 pnpm（推荐）
pnpm install
```

#### 2. 配置环境变量

复制环境变量文件并根据实际情况修改：

```bash
# 开发环境
cp .env.development .env

# 编辑 .env 文件，设置后端API地址
# Python 后端（默认）
VITE_API_BASE_URL=http://localhost:8001

# 或 Java 后端
# VITE_API_BASE_URL=http://localhost:9092
```

**后端服务说明**：
- **Python 后端**：`http://localhost:8001`（LangGraph Agent）
- **Java 后端**：`http://localhost:9092`（agentServer）

#### 3. 启动开发服务器

```bash
# 使用 npm
npm run dev

# 或使用 yarn
yarn dev

# 或使用 pnpm
pnpm dev
```

开发服务器将在 `http://localhost:3000` 启动。

#### 4. 构建生产版本

```bash
# 使用 npm
npm run build

# 或使用 yarn
yarn build

# 或使用 pnpm
pnpm build
```

构建产物将输出到 `dist` 目录。

#### 5. 预览生产构建

```bash
# 使用 npm
npm run preview

# 或使用 yarn
yarn preview

# 或使用 pnpm
pnpm preview
```

## 📁 项目结构

```
frontend/
├── public/                 # 静态资源目录
├── src/
│   ├── components/         # 通用组件目录
│   │   ├── Chat/          # 聊天相关组件
│   │   ├── Dashboard/     # 数据展示相关组件
│   │   └── Layout/        # 布局组件
│   ├── pages/             # 页面组件目录
│   ├── services/          # API服务目录
│   ├── stores/           # 状态管理目录（Zustand）
│   ├── hooks/            # 自定义Hooks目录
│   ├── utils/            # 工具函数目录
│   ├── types/            # TypeScript类型定义目录
│   ├── assets/           # 静态资源目录
│   │   ├── images/
│   │   └── styles/        # 全局样式
│   ├── App.tsx           # 根组件
│   └── main.tsx         # 入口文件
├── .env.development      # 开发环境变量
├── .env.production       # 生产环境变量
├── package.json          # 依赖配置
├── tsconfig.json         # TypeScript配置
├── vite.config.ts        # Vite配置
└── tailwind.config.js    # Tailwind配置
```

## 🛠️ 技术栈

- **框架**: React 18 + TypeScript
- **构建工具**: Vite 5
- **UI组件库**: Ant Design 5.x
- **状态管理**: Zustand
- **HTTP客户端**: Axios
- **路由**: React Router 6
- **图表库**: ECharts
- **样式方案**: Tailwind CSS + CSS Modules
- **日期处理**: Day.js

## 📝 开发规范

### 代码格式化

```bash
# 格式化代码
npm run format
```

### 代码检查

```bash
# 运行 ESLint
npm run lint
```

### 提交规范

建议使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建/工具链相关

## 🔧 配置说明

### Vite配置

开发服务器配置了代理，将 `/api` 和 `/health` 请求代理到后端服务器（默认 `http://localhost:8001`）。

### TypeScript配置

- 启用了严格模式
- 配置了路径别名 `@/*` 指向 `src/*`
- 使用 React 17+ 的 JSX 转换

### Tailwind CSS配置

已配置 Tailwind CSS，可以直接使用工具类进行样式开发。

## 🌐 API配置

### 环境变量

- `VITE_API_BASE_URL`: 后端API基础地址
  - Python 后端（默认）：`http://localhost:8001`
  - Java 后端：`http://localhost:9092`

### API接口

- **聊天接口**: `POST /api/chat`
- **健康检查**: `GET /health`
- **数据库健康检查**: `GET /health/db`

详细接口文档请参考 `docs/设计/V1.0-003-接口设计.md`。

### 后端配置

前端支持对接多个后端服务，详细配置说明请参考 [后端配置说明](./docs/配置/后端配置说明.md)。

## 📚 相关文档

### 设计文档
- [技术方案选型](./docs/设计/V1.0-001-前端技术方案选型.md)
- [功能设计](./docs/设计/V1.0-002-功能设计.md)
- [接口设计](./docs/设计/V1.0-003-接口设计.md)

### 安装与配置
- [安装手册](./docs/安装/安装手册.md)
- [后端配置说明](./docs/配置/后端配置说明.md)

### 开发指南
- [node_modules 详解](./docs/开发指南/node_modules详解.md)

### 项目文档
- [项目搭建说明](./docs/项目/项目搭建说明.md)
- [迁移说明](./docs/项目/迁移说明.md)

## 🐛 常见问题

### 1. 端口被占用

如果 3000 端口被占用，Vite 会自动尝试下一个可用端口。

### 2. 代理不生效

检查 `vite.config.ts` 中的代理配置，确保后端服务正在运行。

### 3. 类型错误

运行 `npm run build` 检查 TypeScript 类型错误。

## 📄 许可证

本项目遵循项目根目录的许可证。

