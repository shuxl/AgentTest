# 环境安装手册

## 📋 目录

1. [系统要求](#系统要求)
2. [Node.js 安装](#nodejs-安装)
3. [包管理器选择](#包管理器选择)
4. [项目依赖安装](#项目依赖安装)
5. [环境变量配置](#环境变量配置)
6. [验证安装](#验证安装)
7. [常见问题](#常见问题)

## 系统要求

### 操作系统

- **macOS**: 10.15+
- **Windows**: Windows 10+
- **Linux**: Ubuntu 18.04+ / CentOS 7+

### 软件要求

- **Node.js**: >= 18.0.0
- **npm**: >= 9.0.0（随 Node.js 安装）
- **Git**: >= 2.0.0（用于版本控制）

## Node.js 安装

### macOS

#### 方法1: 使用 Homebrew（推荐）

```bash
# 安装 Homebrew（如果未安装）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装 Node.js
brew install node

# 验证安装
node --version
npm --version
```

#### 方法2: 使用官方安装包

1. 访问 [Node.js 官网](https://nodejs.org/)
2. 下载 LTS 版本（推荐）
3. 运行安装包并按照提示完成安装

#### 方法3: 使用 nvm（Node Version Manager）

```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 重新加载 shell 配置
source ~/.zshrc  # 或 ~/.bash_profile

# 安装 Node.js LTS 版本
nvm install --lts
nvm use --lts

# 验证安装
node --version
```

### Windows

#### 方法1: 使用官方安装包（推荐）

1. 访问 [Node.js 官网](https://nodejs.org/)
2. 下载 Windows 安装包（.msi）
3. 运行安装包，按照提示完成安装
4. 重启命令提示符或 PowerShell

#### 方法2: 使用 Chocolatey

```powershell
# 以管理员身份运行 PowerShell
choco install nodejs-lts

# 验证安装
node --version
npm --version
```

### Linux (Ubuntu/Debian)

```bash
# 更新包列表
sudo apt update

# 安装 Node.js 和 npm
sudo apt install nodejs npm

# 验证安装
node --version
npm --version
```

### Linux (CentOS/RHEL)

```bash
# 安装 Node.js 和 npm
sudo yum install nodejs npm

# 验证安装
node --version
npm --version
```

## 包管理器选择

项目支持三种包管理器，推荐使用 **pnpm**（速度更快，磁盘占用更少）。

### pnpm 安装

```bash
# 使用 npm 安装 pnpm
npm install -g pnpm

# 验证安装
pnpm --version
```

### yarn 安装

```bash
# 使用 npm 安装 yarn
npm install -g yarn

# 验证安装
yarn --version
```

### npm

npm 随 Node.js 一起安装，无需额外安装。

## 项目依赖安装

### 1. 进入项目目录

```bash
cd langGraphFlow/V2_0_Agent/frontend
```

### 2. 安装依赖

#### 使用 pnpm（推荐）

```bash
pnpm install
```

#### 使用 yarn

```bash
yarn install
```

#### 使用 npm

```bash
npm install
```

### 3. 安装过程说明

安装过程可能需要几分钟时间，具体取决于网络速度。安装过程中会：

- 下载所有依赖包
- 解析依赖关系
- 构建依赖包（如果需要）

### 4. 安装完成标志

安装成功后，你会看到类似以下输出：

```
✔ Packages installed successfully
```

同时会在项目根目录生成 `node_modules` 目录。

## 环境变量配置

### 1. 创建环境变量文件

项目根目录下已有 `.env.development` 和 `.env.production` 文件。

开发环境可以直接使用 `.env.development`，或创建 `.env` 文件：

```bash
# 复制开发环境配置
cp .env.development .env
```

### 2. 配置 API 地址

编辑 `.env` 文件，设置后端 API 地址：

```bash
# .env
VITE_API_BASE_URL=http://localhost:8001
```

**注意**: 
- 开发环境默认使用 `http://localhost:8001`
- 生产环境需要根据实际部署情况修改 `.env.production`

### 3. 环境变量说明

| 变量名 | 说明 | 默认值 | 必需 |
|--------|------|--------|------|
| `VITE_API_BASE_URL` | 后端API基础地址 | `http://localhost:8001` | 是 |

## 验证安装

### 1. 检查 Node.js 版本

```bash
node --version
# 应该显示 v18.x.x 或更高版本
```

### 2. 检查依赖安装

```bash
# 使用 pnpm
pnpm list --depth=0

# 或使用 yarn
yarn list --depth=0

# 或使用 npm
npm list --depth=0
```

### 3. 启动开发服务器

```bash
# 使用 pnpm
pnpm dev

# 或使用 yarn
yarn dev

# 或使用 npm
npm run dev
```

如果一切正常，你会看到：

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

在浏览器中访问 `http://localhost:3000`，应该能看到应用界面。

### 4. 运行构建

```bash
# 使用 pnpm
pnpm build

# 或使用 yarn
yarn build

# 或使用 npm
npm run build
```

构建成功后，会在 `dist` 目录生成生产构建文件。

## 常见问题

### 1. Node.js 版本过低

**错误信息**: 
```
Error: The engine "node" is incompatible with this module
```

**解决方法**:
- 升级 Node.js 到 18.0.0 或更高版本
- 使用 nvm 管理多个 Node.js 版本

### 2. 依赖安装失败

**错误信息**: 
```
npm ERR! code ENOTFOUND
npm ERR! errno ENOTFOUND
```

**解决方法**:
- 检查网络连接
- 尝试使用国内镜像源：
  ```bash
  # 使用淘宝镜像
  npm config set registry https://registry.npmmirror.com
  
  # 或使用 pnpm
  pnpm config set registry https://registry.npmmirror.com
  ```

### 3. 权限错误（macOS/Linux）

**错误信息**: 
```
EACCES: permission denied
```

**解决方法**:
- 使用 `sudo`（不推荐）
- 或配置 npm 使用其他目录：
  ```bash
  mkdir ~/.npm-global
  npm config set prefix '~/.npm-global'
  export PATH=~/.npm-global/bin:$PATH
  ```

### 4. 端口被占用

**错误信息**: 
```
Port 3000 is in use, trying another one...
```

**解决方法**:
- 关闭占用 3000 端口的程序
- 或修改 `vite.config.ts` 中的端口配置

### 5. 代理不生效

**问题**: 无法连接到后端 API

**解决方法**:
- 确保后端服务正在运行
- 检查 `vite.config.ts` 中的代理配置
- 检查 `.env` 文件中的 `VITE_API_BASE_URL` 配置

### 6. TypeScript 类型错误

**错误信息**: 
```
Type error: ...
```

**解决方法**:
- 运行 `npm run build` 查看详细错误信息
- 检查 `tsconfig.json` 配置
- 确保所有类型定义文件存在

## 下一步

安装完成后，请参考 [README.md](../../README.md) 了解如何使用项目。

## 获取帮助

如果遇到其他问题，请：

1. 查看项目文档
2. 检查 GitHub Issues
3. 联系项目维护者

