# 公开数据集使用说明

## 📊 数据集适用性分析

README.md 中提到的公开数据集对不同用途的适用性：

### 1. Wikipedia 向量数据集 ✅ **推荐用于文本向量生成**

#### 适用性：⭐⭐⭐⭐⭐
- ✅ **适合文本向量生成**：Wikipedia 是真实的文本数据，经过 embedding 后可用于真实场景测试
- ✅ **数据量大**：可以生成百万级数据
- ✅ **文本质量高**：真实的百科文本，具有语义相关性
- ✅ **适合精度评估**：有真实语义关系的文本可以评估查询精度（召回率）

#### 使用方式：

##### 方式1：使用内置下载工具（⭐ 推荐用于小规模数据）

**适用场景**：1000 - 50000 条数据（适合学习和测试）

**优点**：
- ✅ 无需额外工具
- ✅ 自动分块处理
- ✅ 支持多种下载模式
- ✅ 简单易用

**缺点**：
- ❌ 不适合大规模数据（百万级）
- ❌ API 有速率限制
- ❌ 下载速度相对较慢

使用 `download_wikipedia.py` 工具直接从 Wikipedia API 下载：

```bash
# 1. 下载随机页面（推荐用于快速测试）
# 文件自动保存到 data_generation/download_wikipedia/ 目录
# 文件名自动生成：wikipedia_random_100_20250101_120000.txt
python data_generation/download_wikipedia.py \
    --random 100

# 2. 按关键词搜索下载
# 文件名：wikipedia_search_人工智能_20250101_120000.txt
python data_generation/download_wikipedia.py \
    --search "人工智能" \
    --limit 50

# 3. 按分类下载（如计算机科学）
# 文件名：wikipedia_category_计算机科学_20250101_120000.txt
python data_generation/download_wikipedia.py \
    --category "Category:计算机科学" \
    --limit 100


# 3.1. 🔍 查找Wikipedia上有哪些分类可用（新增功能）
# 方式1：搜索包含关键词的分类（如搜索医疗相关的分类）
python data_generation/download_wikipedia.py \
    --find-categories "医疗"

# 方式2：列出某个分类的子分类（如查看"Category:医疗"的子分类）
python data_generation/download_wikipedia.py \
    --list-subcategories "Category:医疗"

# 方式3：查看某个分类下的所有成员（子分类和页面）
python data_generation/download_wikipedia.py \
    --list-category "Category:医疗"

# 4. 从标题列表文件下载
# 先创建 titles.txt，每行一个页面标题
echo -e "人工智能\n机器学习\n深度学习" > titles.txt
python data_generation/download_wikipedia.py \
    --titles titles.txt

# 5. 自定义输出文件路径（可选）
python data_generation/download_wikipedia.py \
    --random 100 \
    --output my_custom_file.txt

# 6. 调整超时和重试参数（网络慢时使用）
python data_generation/download_wikipedia.py \
    --random 100 \
    --timeout 60 \
    --retry 5
```

**重要说明**：
- ✅ `--output` 参数**可选**：不指定时自动保存到 `data_generation/download_wikipedia/` 目录
- ✅ 自动生成带时间戳的文件名，避免覆盖
- ✅ 默认文本分块大小：512 字符（可通过 `--chunk-size 0` 取消分块）
- ✅ 默认超时：30秒，重试：3次（网络慢时可调整）

然后生成向量：
```bash
# 方式1：使用自动生成的文件（推荐）
# 文件保存在 data_generation/download_wikipedia/ 目录
python data_generation/generate_text_vectors.py \
    --file data_generation/download_wikipedia/wikipedia_random_100_*.txt

# 方式2：使用完整路径
python data_generation/generate_text_vectors.py \
    --file data_generation/download_wikipedia/wikipedia_random_100_20250101_120000.txt

# 方式3：如果指定了自定义输出路径
python data_generation/generate_text_vectors.py \
    --file my_custom_file.txt
```

##### 方式2：从 Wikipedia 转储下载（适合大规模数据）

**适用场景**：100000+ 条数据（百万级数据）

**为什么不用 API 方式？**
- ❌ **效率低**：API 方式需要逐个请求，百万级数据可能需要数天
- ❌ **速率限制**：Wikipedia API 有速率限制，容易触发限制
- ❌ **不稳定**：大量请求可能导致连接问题
- ✅ **转储方式**：一次性下载压缩文件，本地提取，效率高

**转储文件大小参考**：
- 中文 Wikipedia 完整转储：约 2-3 GB（压缩后）
- 解压后：约 10-15 GB
- 文章数量：约 100-200 万篇

如果需下载大量数据（百万级），**必须**使用 Wikipedia 转储：

1. 下载 Wikipedia 转储：
   ```bash
   # 下载中文 Wikipedia 示例
   wget https://dumps.wikimedia.org/zhwiki/latest/zhwiki-latest-pages-articles.xml.bz2
   ```

2. 提取文本（使用 WikiExtractor）：
   ```bash
   pip install wikiextractor
   wikiextractor --json zhwiki-latest-pages-articles.xml.bz2 -o output_dir
   ```

3. 提取文本内容并生成文件：
   ```python
   # 示例脚本提取文本
   import json
   texts = []
   for file in Path('output_dir').rglob('*'):
       if file.is_file():
           with open(file, 'r') as f:
               for line in f:
                   data = json.loads(line)
                   texts.append(data['text'])
   
   # 保存为文本文件
   with open('wikipedia_texts.txt', 'w') as f:
       for text in texts:
           f.write(text + '\n')
   ```

4. 生成向量：
   ```bash
   python data_generation/generate_text_vectors.py --file wikipedia_texts.txt --chunk-size 512
   ```

##### 方式3：使用示例实现（仅用于测试）
```bash
python data_generation/generate_text_vectors.py --wikipedia wiki_sample.txt --count 1000
```

---

### 2. SIFT1M / GIST1M ❌ **不适合文本向量生成**

#### 适用性：❌（不适合文本）
- ❌ **不是文本数据**：SIFT 和 GIST 是图像特征向量
  - SIFT：128维图像关键点特征
  - GIST：960维场景描述符
- ⚠️ **维度不匹配**：与文本 embedding（通常 384/768维）维度不同
- ✅ **可用作向量索引测试**：虽然不适合文本向量，但可以作为预计算向量直接用于索引性能测试

#### 使用方式（作为预计算向量）：
```bash
# 1. 下载 SIFT1M 数据集（需要转换格式）
# 从 http://corpus-texmex.irisa.fr/ 下载
# 2. 转换为 NumPy 格式或文本格式
# 3. 直接加载（跳过 embedding）
python data_generation/generate_text_vectors.py --vectors sift1m_vectors.npy
```

#### 注意事项：
- ⚠️ **维度警告**：SIFT1M (128维) 和 GIST1M (960维) 与文本 embedding 维度不同
- ⚠️ **无法评估文本精度**：因为没有原始文本，无法评估文本查询的召回率
- ✅ **可测试索引性能**：可以测试索引构建时间、索引大小、查询速度等性能指标

---

### 3. Ann-benchmarks 数据集 ⚠️ **部分适合**

#### 适用性：取决于具体数据集
- ✅ **部分数据集适合文本向量**：Ann-benchmarks 包含多种数据集
  - 有些是文本向量（如 glove-100）
  - 有些是图像特征（如 sift）
- ⚠️ **需要确认维度**：不同数据集维度不同
- ✅ **标准化测试数据**：适合对比不同系统的性能

#### 使用方式：
```bash
# 1. 从 https://github.com/erikbern/ann-benchmarks 下载数据集
# 2. 确认数据集类型和维度
# 3. 如果维度匹配，直接加载
python data_generation/generate_text_vectors.py --vectors ann_benchmark_vectors.npy
```

---

## 🎯 推荐使用策略

### 策略1：文本向量生成（用于真实场景测试）

**推荐数据集**：
1. **Wikipedia** ⭐⭐⭐⭐⭐
   - 真实文本数据
   - 数据量大
   - 适合精度评估

2. **自定义文本文件** ⭐⭐⭐⭐
   - 使用自己的文档、新闻、技术文档等
   - 完全控制数据质量

**使用方法**：
```bash
# 从文本文件生成
python data_generation/generate_text_vectors.py --file my_texts.txt

# 从目录生成
python data_generation/generate_text_vectors.py --dir ./documents --chunk-size 512
```

---

### 策略2：预计算向量（用于性能测试）

**推荐数据集**：
1. **SIFT1M / GIST1M** ⭐⭐⭐⭐
   - 经典基准数据集
   - 可直接加载，无需 embedding
   - 适合测试索引性能

2. **Ann-benchmarks** ⭐⭐⭐⭐
   - 标准化测试数据
   - 便于对比性能

**使用方法**：
```bash
# 直接加载预计算向量（跳过 embedding）
python data_generation/generate_text_vectors.py --vectors sift1m_vectors.npy
```

**注意事项**：
- 向量维度必须匹配或可接受
- 无法进行文本查询精度评估
- 仅能测试索引性能指标

---

## 📝 数据集对比表

| 数据集 | 适合文本向量生成 | 适合性能测试 | 维度 | 数据量 | 精度评估 |
|--------|----------------|------------|------|--------|---------|
| **Wikipedia** | ✅ 是 | ✅ 是 | 768 (embedding后) | 百万级+ | ✅ 可以 |
| **SIFT1M** | ❌ 否 | ✅ 是 | 128 | 100万 | ❌ 否（无文本） |
| **GIST1M** | ❌ 否 | ✅ 是 | 960 | 100万 | ❌ 否（无文本） |
| **Ann-benchmarks** | ⚠️ 部分 | ✅ 是 | 多种 | 多种 | ⚠️ 取决于数据集 |

---

## 💡 最佳实践建议

### 对于文本向量生成和精度评估（推荐）：

1. **使用 Wikipedia 文本（根据数据量选择方式）**：
   ```bash
   # 小规模（1000-50000 条）：使用 API 工具
   # 1. 下载文本（文件自动保存到 data_generation/download_wikipedia/）
   python data_generation/download_wikipedia.py --random 5000
   
   # 2. 生成向量（使用自动生成的文件名，或指定具体文件）
   python data_generation/generate_text_vectors.py \
       --file data_generation/download_wikipedia/wikipedia_random_5000_*.txt
   
   # 大规模（100000+ 条）：必须使用转储方式（见上面的转储下载步骤）
   # 转储方式效率高 1000+ 倍，API 方式不适合
   ```

2. **使用自己的文本数据**：
   ```bash
   # 从目录读取文档
   python data_generation/generate_text_vectors.py --dir ./my_documents
   ```

### 对于索引性能测试（无精度要求）：

1. **使用 SIFT1M / GIST1M**（如果维度匹配）：
   ```bash
   python data_generation/generate_text_vectors.py --vectors sift1m.npy
   ```

2. **使用随机向量**（快速测试）：
   ```bash
   python data_generation/generate_test_data.py --count 10000
   ```

---

## 🔗 数据集下载链接

### Wikipedia

#### API 方式（适合小规模：1000-50000 条）
- **工具**：使用 `download_wikipedia.py`
- **特点**：
  - 支持随机页面、关键词搜索、分类下载
  - 无需安装额外工具
  - **文件自动保存**到 `data_generation/download_wikipedia/` 目录
  - **自动生成带时间戳的文件名**（无需手动指定 `--output`）
  - 支持超时和重试配置（`--timeout`、`--retry`）
  - 自动文本分块处理（默认 512 字符）
- **基本用法示例**：
  ```bash
  # 最简单的用法：下载 100 个随机页面
  python data_generation/download_wikipedia.py --random 100
  # 文件自动保存为：data_generation/download_wikipedia/wikipedia_random_100_YYYYMMDD_HHMMSS.txt
  ```
- **限制**：
  - ❌ 不适合大规模数据（超过 50000 条效率极低）
  - API 有速率限制
  - 下载速度相对较慢

#### 转储方式（适合大规模：100000+ 条）
- **适用场景**：需要百万级数据时
- **官方网站**：
  - **所有语言转储**：https://dumps.wikimedia.org/
  - **中文 Wikipedia**：https://dumps.wikimedia.org/zhwiki/latest/
- **提取工具**：https://github.com/attardi/wikiextractor
- **数据规模**：
  - 中文完整转储：约 2-3 GB（压缩），100-200 万篇文章
  - 解压后：约 10-15 GB
- **为什么不用 API？**
  - API 方式下载百万级数据可能需要数天
  - 转储方式一次性下载，本地提取，效率高 1000+ 倍

### SIFT1M / GIST1M
- **下载地址**：http://corpus-texmex.irisa.fr/
- **格式说明**：通常为二进制格式，需要转换为 NumPy 或文本格式

### Ann-benchmarks
- **GitHub**：https://github.com/erikbern/ann-benchmarks
- **数据集列表**：https://github.com/erikbern/ann-benchmarks#datasets

---

## 📌 总结

### ✅ 适合用于文本向量生成的数据集：
1. **Wikipedia** - 最佳选择
2. **自定义文本文件** - 灵活实用

### ❌ 不适合用于文本向量生成的数据集：
1. **SIFT1M / GIST1M** - 图像特征，不是文本
2. **其他预计算向量** - 无原始文本，无法评估精度

### ⚠️ 注意事项：
- 文本向量需要原始文本才能评估查询精度（召回率）
- 预计算向量只能测试索引性能，无法评估文本查询精度
- 选择数据集时要考虑向量维度是否匹配你的模型

